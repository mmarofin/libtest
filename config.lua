maro = {}

-- If set to true, the radial menu will be shown when the player holds the 'radial' key.
maro.showradial = true

maro.commands = {
    alert = "libalert"
    context = "libcontext"
    dialog = "libdialog"
    menu = "menu"
    noti = "libnotify"
    prog = "libprog"
    progcircle = "libprogcircle"
    skill = "libskillcheck"
    textui = "libtextui"
}
